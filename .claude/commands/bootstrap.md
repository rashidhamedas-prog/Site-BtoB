description: Full project bootstrap \& capability scanallowed-tools: Read, Glob, Grep, Bash, WebFetch, TodoWrite

Run the Bootstrap Protocol from CLAUDE.md:



1.Inspect project (spawn subagents per layer).

2.Detect required capabilities.

3.Discover best Skills/MCPs/SDKs.

4.Apply quality gates.

5.Install + verify.

6.Update .claude/memory.json.

7.Output the readiness report.Use plan mode; confirm before installing anything.

