# گزارش جلسه — فاز ۱ siteup (شیشه‌ای + تم)

**تاریخ:** 2026-07-22  
**منبع:** siteup.md بخش ۱

## انجام‌شده
1. توکن‌های CSS شیشه‌ای روی برند سبز/طلایی (بدون عوض کردن پالت)
2. اعمال glass روی Modal، کارت‌های «چرا ترنم»، بنر CTA، دکمه `glass`
3. API گروه `theme` + تب ادمین «تنظیمات تم ترنم»
4. `ThemeRuntime`: اعمال رنگ/blur/حالت نمایش + پاپ‌آپ بوتیک و خبرنامه

## تأیید
- `tsc --noEmit` برای `apps/api` و `apps/web` — موفق

## بعدی (فاز ۲ — پس از تأیید شما)
- منوی داینامیک Drag & Drop + Mega Menu
- دکمه «کلکسیون لینن ترنم» → `/linen-collection`
